[Unit]
Description=Control My Server Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/etc/control_my_server_bot
ExecStart=/usr/bin/control_my_server_bot
Restart=always
RestartSec=5

# Elevated scheduling and priority to remain responsive under heavy load
Nice=-10
CPUSchedulingPolicy=rr
CPUSchedulingPriority=1
IOSchedulingClass=realtime
IOSchedulingPriority=4
OOMScoreAdjust=-1000

# Load environment variables from /etc/control_my_server_bot/.env
# godotenv will automatically load it from the WorkingDirectory

[Install]
WantedBy=multi-user.target
