linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

run:
  tests: false

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
