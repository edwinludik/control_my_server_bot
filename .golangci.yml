linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

run:
  tests: false
  version: 1

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
